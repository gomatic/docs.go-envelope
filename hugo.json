{
  "baseURL": "https://gomatic.github.io/docs.go-envelope/",
  "title": "go-envelope",
  "publishDir": "_site",
  "enableRobotsTXT": true,
  "markup": {
    "goldmark": {
      "renderHooks": {
        "link": { "useEmbedded": "fallback" },
        "image": { "useEmbedded": "fallback" }
      }
    }
  },
  "params": {
    "description": "Public documentation for gomatic/go-envelope — envelope (hybrid) encryption and signing for messages."
  }
}
